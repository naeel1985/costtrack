// Re-export shim: the implementation now lives in @cashflow/core, shared with
// the mobile app. Kept so existing "@/lib/cashflow-timeline" imports keep working.
export * from "@cashflow/core/cashflow-timeline";
