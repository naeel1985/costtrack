import { PageLoader } from "@/components/page-loader";

// Shown in the content area (the sidebar/shell stays) while an app page loads.
export default function AppLoading() {
  return <PageLoader />;
}
