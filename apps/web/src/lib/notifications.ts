// Re-export shim: the implementation now lives in @cashflow/core, shared with
// the mobile app. Kept so existing "@/lib/notifications" imports keep working.
export * from "@cashflow/core/notifications";
