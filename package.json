{
  "name": "cashflow-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "vendor:core": "node scripts/vendor-core.mjs",
    "dev": "npm --prefix apps/web run dev",
    "build": "npm --prefix apps/web run build",
    "start": "npm --prefix apps/web run start",
    "lint": "npm --prefix apps/web run lint",
    "test": "npm --workspace packages/core run test && npm --prefix apps/web run test",
    "typecheck": "npm --workspace packages/core run typecheck && npm --prefix apps/web exec tsc -- --noEmit"
  }
}
